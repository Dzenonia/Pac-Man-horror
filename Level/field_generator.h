#include <bits/stdc++.h>


std::vector<std::vector<bool>> generateLevel(int height, int width) {
//    srand(time(0));
//    std::vector<std::vector<bool>> graph(height, std::vector<bool>(width, 1));
//    graph[1][1] = 0;
//    const int MAX_ITERATIONS = 10;
//    for (int i = 0; i < MAX_ITERATIONS; ++i) {
//        int x = rand() % height;
//        int y = rand() % width;
//        if (x == 0 || x == height - 1 || y == 0 || y == width - 1) {
//            continue;
//        }
//        graph[x][y] = 0;
//    }
}